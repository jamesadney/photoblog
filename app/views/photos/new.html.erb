<div class="row">
  <h1 class="header">Upload a new photo.</h1>
</div>
<div class="row">
  <%= simple_form_for [current_user, @photo], html: { multipart: true } do |f| %>
    <%= f.input :image, label: false %>
    <%= f.input :caption %>
    <%= f.button :submit, "Upload" %>
  <% end %>
</div>
