<div class="row">
  <h1 class="header">Users</h1>
  <ul class="large-8 columns">
    <% @users.each do |user| %>
      <li><%= link_to user.username, user_photos_path(user) %></li>
    <% end %>
  </ul>
</div>
